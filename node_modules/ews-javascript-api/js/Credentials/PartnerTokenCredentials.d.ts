import { Uri } from "../Uri";
import { WSSecurityBasedCredentials } from "./WSSecurityBasedCredentials";
export declare class PartnerTokenCredentials extends WSSecurityBasedCredentials {
    private static WsSecuritySymmetricKeyPathSuffix;
    NeedSignature: boolean;
    private keyInfoNode;
    AdjustUrl(url: Uri): Uri;
    Sign(memoryStream: any): any;
}
