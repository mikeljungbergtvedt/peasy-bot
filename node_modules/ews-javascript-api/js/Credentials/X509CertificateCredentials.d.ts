import { Uri } from "../Uri";
import { WSSecurityBasedCredentials } from "./WSSecurityBasedCredentials";
export declare class X509CertificateCredentials extends WSSecurityBasedCredentials {
    private static BinarySecurityTokenFormat;
    private static KeyInfoClauseFormat;
    private static WsSecurityX509CertPathSuffix;
    NeedSignature: boolean;
    private certificate;
    private keyInfoClause;
    AdjustUrl(url: Uri): Uri;
    Sign(memoryStream: any): any;
    ToString(): string;
}
