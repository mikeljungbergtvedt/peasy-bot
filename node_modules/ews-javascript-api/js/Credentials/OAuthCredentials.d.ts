import { IXHROptions } from "../Interfaces";
import { ExchangeCredentials } from "./ExchangeCredentials";
export declare class OAuthCredentials extends ExchangeCredentials {
    private static BearerAuthenticationType;
    private token;
    private credentials;
    private static validTokenPattern;
    constructor(token: string);
    constructor(token: string, verbatim: boolean);
    PrepareWebRequest(request: IXHROptions): void;
}
