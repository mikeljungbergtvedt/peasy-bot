import { WebClientUrl } from "./WebClientUrl";
/**
 * Represents a user setting that is a collection of Exchange web client URLs.
 * @sealed
 */
export declare class WebClientUrlCollection {
    private urls;
    /**
     * Gets the URLs.
     */
    get Urls(): WebClientUrl[];
}
