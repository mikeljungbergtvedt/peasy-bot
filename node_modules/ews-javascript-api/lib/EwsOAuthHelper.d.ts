import { AuthenticationResult } from '@azure/msal-node';
export declare class EwsOAuthHelper {
    #private;
    get cachedToken(): AuthenticationResult;
    constructor({ clientId, clientSecret, tenantId, scopes, loginUrl }: {
        clientId: string;
        clientSecret: string;
        tenantId: string;
        scopes?: string[];
        loginUrl?: string;
    });
    getAppAccessToken(options?: {
        cache?: boolean;
        raw: false;
    }): Promise<string>;
    getAppAccessToken(options?: {
        cache?: boolean;
        raw: true;
    }): Promise<AuthenticationResult>;
}
